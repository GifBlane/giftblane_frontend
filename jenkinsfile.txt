pipeline {
    agent any
    tools {nodejs "node"}
 
    stages {
        stage('build'){
            step{
                git 'https://github.com/GifBlane/giftblane_frontend.git'
                bat 'npm install'
            }
        }
    }
}
