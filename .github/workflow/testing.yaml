# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-nodejs

name: Testing CI

on:
  push:
    branches: ["testing"]
  pull_request:
    branches: ["testing"]

jobs:
  build:
    runs-on: self-hosted
    
    strategy:
      matrix:
        node-version: [18.x]

    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
          cache: "npm"
      - run: npm CI
      - run: npm build
      - uses: actions/ssh-remote@v1
        with:
          host: ${{ secrets.HOSTNAMETEST }}
          port: ${{ secrets.PORTTEST }}
          username: ${{ secrets.USERNAMETEST }}
          private-key: ${{ secrets.PRIVATE_KEYTEST }}
        run: |
          rsync -azv --exclude '.git' . ${{ secrets.USERNAMETEST }}@${HOSTNAMETEST}:${DESTINATION_PATHTEST}